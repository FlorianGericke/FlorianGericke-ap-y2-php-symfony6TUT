{% extends 'base.html.twig' %}

{% block title %}
    SymphArt article
{% endblock %}

{% block body %}
    <div class="container w-min">
        <h1>{{ article.title }}</h1>
        <p>{{ article.body }}</p>
        <p>{{ article.quality }}</p>
        <hr>
        <a href="/" class="btn-primary inline-block align-middle">Go Back</a>
    </div>

{% endblock %}
